# Nodejs API

# Setup

criei um arquivo .env na raiz do projeto com as seguintes variáveis:

```text
NODE_API_PORT = 3000
DATABASE_HOST = mysql-db
DATABASE_USER = test
DATABASE_PASSWORD = test
DATABASE_NAME = nodedb
```

ou renomeie o arquivo .env.example para .env


# Buil & Run

> docker-compose up -d